//takes an array of strings and returns a string containing the html for an unordered list where each list item is an element of the array
function arrayToUl(array) {
  var empStr = "";
  var open = "<html"


}
